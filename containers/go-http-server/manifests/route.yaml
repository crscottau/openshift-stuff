kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: go-http-server
  namespace: test
spec:
  to:
    kind: Service
    name: go-http-server
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
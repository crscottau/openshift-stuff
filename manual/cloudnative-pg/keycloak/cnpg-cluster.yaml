apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: keycloak-pg-cluster
  namespace: keycloak
spec:
  instances: 1

  bootstrap:
    initdb:
      database: keycloak
      owner: keycloak

  storage:
    size: 50Gi


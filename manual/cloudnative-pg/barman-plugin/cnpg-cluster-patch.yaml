apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: test
  namespace: db-test
spec:
  plugins:
  - name: barman-cloud.cloudnative-pg.io
    isWALArchiver: true
    parameters:
      barmanObjectName: mcg-store

apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: quay-ondemand-backup
  namespace: quay
spec:
  cluster:
    name: quay-registry-quay-postgres
    namespace: quay
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
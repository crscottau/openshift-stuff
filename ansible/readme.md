# Ansible cluster build on RHDP VMWare Open Environment

## Generate install-config.yaml

Generate the install-config.yaml file for the RHPD VMWare Open Environment OpenShift install

Varies based on:

- version
- cluster ID
- vSphere password
- API and Ingress VIP

